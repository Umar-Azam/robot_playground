# %%

print("Hello World")
# %%
import numpy as np

# %%
# len = 10
# np.random.seed(42)
# arr_x_y = np.random.rand(len,len)
# arr_x_y = arr_x_y / np.sum(arr_x_y)
# # Probabilities P(x,y) = P(x | y) P(y) = P(y | x)P(x)
# # P(x | y) = P(x,y)/P(y)
# # P(y) = sum (P(x , y),x)
# arr_y = np.zeros(len)

# for i in range(len):
#     arr_y[i] = np.sum(arr_x_y[:,i])

# arr_xCy = np.zeros(shape = arr_x_y.shape)

# for i in range(len):
#     arr_xCy[:,i] = arr_x_y[:,i]  / arr_y[i]
# %%


# %%
